#include <bits/stdc++.h>
#pragma GCC optimize ("O3")
using namespace std;
const int maxn=100005;
int a[maxn],n,q,k,m;
int main() {
    ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    /*cin >> n >> q;
    for(int i=1; i<=n; i++) cin >> a[i];
    while(q--) {
        cin >> k >> m;
        vector<int>ans;
        for(int i=1; i<=n; i++) {
            if(a[i]<=k) ans.push_back(a[i]);
            else {
                int t=a[i]-k;
                ans.push_back(t-k);
            }
        }
        sort(ans.begin(),ans.end());
        int kq=0;
        for(int i=0; i<m; i++) kq+=ans[i];
        cout << kq << "\n";
    }*/
   cout << "a";
}