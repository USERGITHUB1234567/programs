#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n;
    cin >> n;
    long long a[n];
    for(long long i=0; i<n; i++) cin >> a[i];
    stack<long long>st;
    st.push(a[0]);
    for(long long i=1; i<n; i++) {

    }
}
