for(int i:bucket[post]) {
            //     auto[x,y]=qu[i];
            //     int s=fen.query(y)-fen.query(x-1);
            //     if(s>=post) l[i]=post;
            //     else r[i]=post-1;
            // }