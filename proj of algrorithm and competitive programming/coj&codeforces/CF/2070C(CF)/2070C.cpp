#include <bits/stdc++.h>
#pragma GCC optimize("O3")
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    int t;
    cin >> t;
    while(t--) {
        int n,k;
        long long r=0
        cin >> n >> k;
        string s;
        cin >> s;
        long long a[n];
        for(int i=0; i<n; i++) {
            cin >> a[i];
            t+=a[i];
        }
        long long l=0;
        while(l>=r) {
            long long mid=(l+r)>>1;

        }
    }
}
