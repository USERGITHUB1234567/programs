#include <bits/stdc+++.h>
#pragma GCC optimize("O3")
using namespace std;

int main()
{
    long long n,l;
    cin >> n >> l;
    long long a[n],b[n];
    for(int i=0; i<n; i++) cin >> a[i] >> b[i];

}
