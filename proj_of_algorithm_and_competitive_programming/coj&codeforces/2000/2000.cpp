#include <bits/stdc++.h>
#pragma GCC optimize("O3")
using namespace std;
int q;
int main()
{
    ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    cin >> q;
    while(q--) {
        long long t,n,k;
        cin >> t >> n >> k

    }
}
