#include <bits/stdc++.h>
using namespace std;

int main(int argc, char** argv) {
    int tc;cin >> tc;
    while(tc--) {
        int n,k;string s;cin >> n >> k >> s;
        bool rem[n+1];
        for(int i=1; i<=k; ++i) {
            
        }
    }
}