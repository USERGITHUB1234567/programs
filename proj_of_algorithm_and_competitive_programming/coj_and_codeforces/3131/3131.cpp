#pragma GCC optimize("O3")
#include <bits/stdc++.h>
#define file(name) freopen(name ".inp", "r", stdin); freopen(name ".out", "w", stdout);
using namespace std;
const int maxn=100005,maxt=102;
int n,t,k,a[maxn],c[maxt][maxt];
int main() {
    ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    cin >> n >> t >> k;
    for(int i=1; i<=t; i++) {
        for(int j=1; j<=t; j++) cin >> c[i][j];
    }
    for(int i=1; i<=n; i++) cin >> a[i];
    int ck=0;
    for(int i=1; i<=n; i++) {
        
    }
}