#include <bits/stdc++.h>
using namespace std;

int main(int argc, char** argv) {
    int tc;cin >> tc;
    while(tc--) {
        int n,k,pos;cin >> n >> k;
        bool a[n];
        vector<bool>v;
        for(int i=1; i<=n; ++i) {
            cin >> a[i-1];
        }
        cin >> pos;
        // for(int i=1; i<=n; ++i) {
        //     if(pos==i) pos=v.size();
        //     if(v.empty() || v.back()!=a[i-1]) v.push_back(a[i]);
        // }
        
    }
}