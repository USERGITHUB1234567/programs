#include <bits/stdc++.h>
#pragma GCC optimize("O3")
using namespace std;
char ch[2001][2001];
int main()
{
    ios_base::sync_with_stdio(false);cin.tie(nullptr);cout.tie(nullptr);
    int r,c;
    cin >> r >> c;
    for(int i=1; i<=r; i++) {
        for(int j=1; j<=c; j++) cin >> ch[i][j];
    }

}
